import WordButton from "./WordButton";
import StoryButton from "./StoryButton";

const ModeButtons = () => {
  return (
    <div>
      <WordButton />
      <StoryButton />
    </div>
  );
};

export default ModeButtons;
