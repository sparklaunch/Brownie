const YouDidItText = () => {
  return (
    <div className={`absolute top-[10%] left-[50%] translate-x-[-50%]`}>
      <img src={`/assets/images/you_did_it.svg`} alt={`You did it text`} />
    </div>
  );
};

export default YouDidItText;
