import { Grid } from "@mui/material";

const CatalogLevel = ({ level }) => {
  return (
    <div>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6} md={4} lg={3}>
          <p>wow</p>
        </Grid>
      </Grid>
    </div>
  );
};

export default CatalogLevel;
