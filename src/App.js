import { Button } from "@mui/material";

const App = () => {
  return (
    <div>
      <p className={`text-5xl`}>Hello</p>
      <Button variant="contained">Primary</Button>
    </div>
  );
};

export default App;
